import { DEFAULT_SITE_URL, OWNER_NAME, SITE_NAME } from "@/lib/constants";

export const siteConfig = {
  siteName: SITE_NAME,
  ownerName: OWNER_NAME,
  currentPhase: "Phase 9C",
  currentState: "Resume PDF Implementation",
  developmentUrl: DEFAULT_SITE_URL,
  productionUrl: null,
};
